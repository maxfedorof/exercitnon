function DotBin(params) {
    // Function body example
    console.log('Received parameters:', params);
    // Perform some operations based on params
}

// Calling the function
DotBin({ option: 'value' });
